export const metadata = {
  title: 'Chapter Profile | HackByteCodex',
  description: 'Explore college chapters, their projects, events, and members on HackByteCodex',
};

export default function ChapterLayout({ children }) {
  return children;
}